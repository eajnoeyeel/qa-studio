# Application
APP_NAME=SaaS CS QA Studio
DEBUG=true
API_PREFIX=/api/v1

# Database (Postgres primary, SQLite for tests only)
DATABASE_URL=postgresql://qa_studio:qa_studio@localhost:5433/qa_studio
# DATABASE_URL=sqlite:///./qa_studio.db

# Langfuse (optional - graceful fallback if not set)
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=https://cloud.langfuse.com

# LLM Provider
# Options: mock, openai, anthropic
LLM_PROVIDER=mock
LLM_MODEL=gpt-4o-mini

# OpenAI (required if LLM_PROVIDER=openai)
OPENAI_API_KEY=

# Anthropic (required if LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=

# RAG Settings
DOCS_PATH=./docs
VECTOR_STORE_PATH=./vector_store
EMBEDDING_MODEL=text-embedding-3-small
RAG_TOP_K=5

# Sampling Thresholds
AB_AMBIGUOUS_THRESHOLD=2.0
LOW_SCORE_THRESHOLD=2
